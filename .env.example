# =============================================================================
# CHAT IA INDEPENDENTE - CONFIGURAÇÃO DE VARIÁVEIS DE AMBIENTE
# =============================================================================
# 
# IMPORTANTE:
# 1. Copie este arquivo para .env (cp .env.example .env)
# 2. Preencha os valores reais para cada variável
# 3. NUNCA commite o arquivo .env no Git (ele já está no .gitignore)
# 
# =============================================================================

# -----------------------------------------------------------------------------
# 🔴 CRÍTICO - SEGURANÇA (OBRIGATÓRIO)
# -----------------------------------------------------------------------------

# Chave secreta para sessões Flask (obrigatório em produção)
SECRET_KEY=your-secret-key-here

# Ambiente da aplicação (development ou production)
FLASK_ENV=production
FLASK_DEBUG=false

# -----------------------------------------------------------------------------
# 🤖 IA (OpenAI/Anthropic)
# -----------------------------------------------------------------------------
DUIMP_AI_ENABLED=true
DUIMP_AI_PROVIDER=openai
DUIMP_AI_API_KEY=sk-your-openai-api-key-here
DUIMP_AI_MODEL=gpt-4o-mini
DUIMP_AI_TIMEOUT=60.0

# -----------------------------------------------------------------------------
# 🗄️ SQL SERVER (Base de Dados Corporativa)
# -----------------------------------------------------------------------------
SQL_SERVER=172.16.10.8\SQLEXPRESS
SQL_USERNAME=sa
SQL_PASSWORD=your-sql-server-password
SQL_DATABASE=Make

# -----------------------------------------------------------------------------
# 🔐 INTEGRA COMEX (SERPRO) - API Bilhetada
# -----------------------------------------------------------------------------
INTEGRACOMEX_BASE_URL=https://apigateway.serpro.gov.br/integra-comex-v1
INTEGRACOMEX_CONSUMER_KEY=your-consumer-key
INTEGRACOMEX_CONSUMER_SECRET=your-consumer-secret

# -----------------------------------------------------------------------------
# 🐘 BANCO DE DADOS LOCAL (Postgres - Recomendado para Docker)
# -----------------------------------------------------------------------------
USE_POSTGRES=true
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=maike_chat
POSTGRES_USER=postgres
POSTGRES_PASSWORD=maike123

# -----------------------------------------------------------------------------
# 📧 EMAIL (Microsoft Graph API - Office 365)
# -----------------------------------------------------------------------------
EMAIL_TENANT_ID=your-tenant-id
EMAIL_CLIENT_ID=your-client-id
EMAIL_CLIENT_SECRET=your-client-secret
EMAIL_DEFAULT_MAILBOX=mIAke@makeconsultores.com.br

# -----------------------------------------------------------------------------
# 🐳 DOCKER
# -----------------------------------------------------------------------------
PORT=5001

# 📊 Queries SQL

Esta pasta contém queries SQL de referência usadas no desenvolvimento.

## 📁 Arquivos

- `querry ce_kanban.sql` - Query para buscar CEs do Kanban
- `querry cct_kanban.sql` - Query para buscar CCTs do Kanban
- `querry di_kanban.sql` - Query para buscar DIs do Kanban
- `querry duimp_kanban.sql` - Query para buscar DUIMPs do Kanban
- `querry-shipgo.sql` - Query para buscar dados do ShipsGo

## 📝 Notas

Estas queries são apenas para referência. O código Python usa funções do `db_manager.py` e `sql_server_adapter.py` para executar queries de forma segura.












